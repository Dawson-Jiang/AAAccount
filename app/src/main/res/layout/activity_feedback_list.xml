<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include android:id="@+id/ctitle" layout="@layout/common_title"/>

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/srefreshRecord"
        android:layout_width="match_parent"
        android:layout_below="@id/ctitle"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/lv_feedback"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="5dip"
            android:layout_marginRight="5dip"
            android:divider="@color/line"
            android:dividerHeight="@dimen/line_hight"/>
    </android.support.v4.widget.SwipeRefreshLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/main_floatbtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add"
        app:layout_behavior="abc"
        app:layout_scrollFlags="scroll|enterAlways" />
</RelativeLayout>